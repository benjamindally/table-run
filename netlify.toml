[build]
  base = "packages/web"
  command = "cd ../.. && pnpm install && pnpm build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA routing - redirect all routes to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
